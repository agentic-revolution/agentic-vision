{
  "name": "@cortex/mcp-server",
  "version": "0.3.1",
  "description": "Model Context Protocol server for Cortex — translates MCP tool calls into Cortex protocol messages over Unix socket",
  "type": "module",
  "license": "Apache-2.0",
  "bin": {
    "cortex-mcp-server": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
